class GPSPoint {
  final double latitude;
  final double longitude;
  final double speed;
  final int timestamp;
 // GPSPoint(this.latitude, this.longitude, this.speed, this.timestamp);

  GPSPoint({
    required this.latitude,
    required this.longitude,
    required this.speed,
    required this.timestamp,
  });

}
